# Any local copy of the record. It is data, not source, and it is measured
# in hundreds of megabytes.
/var

# marimo's per-notebook state and exports.
__marimo__/

# The maintainer's local tooling, as in galata-datawatch.
/.claude/
/openspec/

# Visual-check evidence the chart tool writes beside a chart: screenshots
# and receipts regenerated on demand, not the chart itself.
/design/charts/*.visual-check.*

# No token is ever tracked.
*.gvt
*.gvkit

.DS_Store
/.venv/
__pycache__/
